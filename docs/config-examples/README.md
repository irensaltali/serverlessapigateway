# Canonical Config Examples

These files are the source-of-truth configuration examples for documentation and tests.

- `minimal.json`: Smallest valid configuration.
- `auth0.json`: Auth0 integration and protected route example.
- `supabase.json`: Supabase OTP and protected route example.

All examples are validated in automated tests and docs checks.
